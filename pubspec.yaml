name: youmr_v2
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.0 # 상태 관리
  riverpod_annotation: ^2.3.0
  freezed_annotation: ^2.4.1 # 불변 데이터 클래스
  json_annotation: ^4.8.1 # JSON 직렬화
  cupertino_icons: ^1.0.2
  firebase_core: ^3.15.0
  firebase_auth: ^5.6.1
  cloud_firestore: ^5.6.10
  google_sign_in: ^6.2.1
  youtube_player_flutter: ^9.1.1
  intl: ^0.18.1
  image_picker: ^1.0.7
  uuid: ^4.3.3
  flutter_inappwebview: ^6.1.5
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.6 # 코드 생성
  freezed: ^2.4.5 # Freezed 코드 생성
  riverpod_generator: ^2.3.6 # Riverpod 코드 생성
  json_serializable: ^6.7.1 # JSON 코드 생성
  mocktail: ^1.0.3 # 테스트용 mock
  fake_cloud_firestore: ^3.1.0 # Firestore mock (공식 대체)
  flutter_lints: ^5.0.0


flutter:

  uses-material-design: true

  assets:
    - assets/images/

